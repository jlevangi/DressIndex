{
  "name": "dress-index",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "ensure:deps": "node scripts/ensure-node-modules.mjs",
    "dev": "npm run ensure:deps && vite",
    "build": "npm run ensure:deps && vite build",
    "build:pwa": "npm run ensure:deps && vite build && npx cap sync",
    "preview": "npm run ensure:deps && vite preview"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "firebase": "^12.9.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "@vite-pwa/assets-generator": "^1.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.9.3",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^1.2.0"
  }
}
